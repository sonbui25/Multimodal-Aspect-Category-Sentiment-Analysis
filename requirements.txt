# --- CORE DATA ---
# Numpy < 2.0 là bắt buộc để tương thích tốt nhất với Torch/Pandas bản ổn định
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2
scipy==1.12.0
matplotlib==3.8.4

# --- AI & NLP ---
# Transformers bản 4.40 là đủ mới và ổn định cho Py 3.10
transformers==4.40.0
tokenizers==0.19.1
sentencepiece==0.1.99
nltk==3.8.1
bert-score==0.3.13
rouge-score==0.1.2
safetensors==0.4.2

# --- UTILITIES & SYSTEM ---
# Protobuf 3.20.3 là "Chìa khóa vàng" để tránh lỗi trên Windows
protobuf==3.20.3
click==8.1.7
tqdm==4.66.2
requests==2.31.0
PyYAML==6.0.1
regex==2023.12.25
joblib==1.3.2
filelock==3.13.1
accelerate==0.29.2

# --- OPTIONAL ---
# Bỏ qua underthesea ở đây để tránh lỗi cài đặt (sẽ xử lý bằng code)
underthesea

# pip install torch==2.1.2 torchvision==0.16.2 torchaudio==2.1.2 --index-url https://download.pytorch.org/whl/cu118